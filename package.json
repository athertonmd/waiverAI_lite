{
  "name": "waiver-data-hub-lite",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "infra",
    "lambdas",
    "shared",
    "ui"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
